Place your real logo and background images here to replace the placeholders.

- Logo: replace `logo.svg` with the supplied campus/logo image. Keep the filename `logo.svg` or update paths in `inc/header.php` and `css/style.css` accordingly.
- Background: replace `bg.svg` with the supplied panoramic campus image (e.g., `bg.jpg`). Keep the filename `bg.svg` (or `bg.jpg`) and ensure `css/style.css` references `/Sustain-U/assets/bg.svg`.

Recommended sizes:
- Logo: 300x80px (SVG or PNG).
- Background: at least 1600px wide, saved as JPG or SVG.

After replacing, clear browser cache and reload pages. If you want, I can replace the placeholders with the exact images you attached if you upload them into the project `assets/` folder.
